export interface ICategory {
  title: string;
  imgPath: string;
  urlPath: string;
}

export interface ITypeMeal {
  title: string;
  imgPath: string;
}

export interface IRecipe {
  title: string;
  imgPath: string;
  cookingTime: string;
}
